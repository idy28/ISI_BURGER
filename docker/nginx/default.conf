# server {
#     listen 80;
#     server_name isiburger.local;

#     root /var/www/public;
#     index index.php index.html;

#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     location ~ \.php$ {
#         fastcgi_pass app:9000;
#         fastcgi_index index.php;
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#     }
# }

server {
    listen 80;
    server_name isiburger.local;

    root /var/www/public;
    index index.php index.html;

    # Routes Laravel
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # PHP-FPM
    location ~ \.php$ {
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }

    # Fichiers dans /storage (images, uploads)
    location /storage/ {
        alias /var/www/public/storage/;
        access_log off;
        expires max;
        add_header Cache-Control public;
    }

    # Sécurité : bloquer .env et fichiers cachés
    location ~ /\. {
        deny all;
    }
}